
#include "Chunk.h"
#include <glad/glad.h>
#include <iostream>



void Chunk::generateChunk() {
	for (int x; x <= CHUNK_SIZE; x++) {
		for (int z; z <= CHUNK_SIZE; z++) {
			for (int y; y <= 5; y++) {

			}
		}
	}
}
